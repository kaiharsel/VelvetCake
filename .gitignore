# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.production
.env.local
# Any other env file with secrets (keep an optional committed .env.example)
.env.*
!.env.example
.vercel/
Thumbs.db
desktop.ini

# Firebase (CLI cache & local emulator artifacts).
# NOTE: firebase.json and .firebaserc are config and SHOULD stay tracked.
.firebase/
.runtimeconfig.json
firebase-export-*/

# Claude Code local tooling (agents, skills, configs) — not part of the app.
.claude/
